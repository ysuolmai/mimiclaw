name: Build & Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: espressif/idf:v5.5.2

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build firmware
        shell: bash
        run: |
          . $IDF_PATH/export.sh
          idf.py set-target esp32s3
          idf.py build

      - name: Prepare release artifacts
        shell: bash
        run: |
          . $IDF_PATH/export.sh
          VERSION="${GITHUB_REF_NAME}"

          # Match xiaozhi-esp32s3-supermini packaging: merged firmware contains
          # only bootloader, partition table, and app. Storage/SPIFFS stays separate.
          esptool.py --chip esp32s3 merge_bin \
            --flash_mode dio \
            --flash_freq 80m \
            --flash_size 4MB \
            -o "mimiclaw-esp32-s3-supermini-merged-${VERSION}.bin" \
            0x0      build/bootloader/bootloader.bin \
            0x8000   build/partition_table/partition-table.bin \
            0x20000  build/mimiclaw.bin

          # Copy individual binaries with version suffix
          cp build/mimiclaw.bin              "mimiclaw-esp32-s3-supermini-${VERSION}.bin"
          cp build/bootloader/bootloader.bin "bootloader-esp32-s3-supermini-${VERSION}.bin"
          cp build/partition_table/partition-table.bin "partition-table-esp32-s3-supermini-${VERSION}.bin"
          cp build/spiffs.bin                "spiffs-esp32-s3-supermini-${VERSION}.bin"

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          body: |
            ## Flashing Guide

            ### 1. Merged firmware (preserves storage)

            Download `mimiclaw-esp32-s3-supermini-merged-${{ github.ref_name }}.bin` and flash it at `0x0`.
            This merged image contains the bootloader, partition table, and app only; it does not include SPIFFS/storage.

            ```bash
            esptool.py --chip esp32s3 -b 460800 write_flash 0x0 mimiclaw-esp32-s3-supermini-merged-${{ github.ref_name }}.bin
            ```

            ### 2. Individual binaries with storage

            ```bash
            esptool.py --chip esp32s3 -b 460800 write_flash \
              0x0      bootloader-esp32-s3-supermini-${{ github.ref_name }}.bin \
              0x8000   partition-table-esp32-s3-supermini-${{ github.ref_name }}.bin \
              0x20000  mimiclaw-esp32-s3-supermini-${{ github.ref_name }}.bin \
              0x2F0000 spiffs-esp32-s3-supermini-${{ github.ref_name }}.bin
            ```

            ### 3. First-time setup

            After flashing, connect via serial (115200 baud) and configure:

            ```
            set_wifi <ssid> <password>
            set_tg_token <your-telegram-bot-token>
            set_api_key <your-anthropic-api-key>
            restart
            ```
          files: |
            mimiclaw-esp32-s3-supermini-merged-${{ github.ref_name }}.bin
            mimiclaw-esp32-s3-supermini-${{ github.ref_name }}.bin
            bootloader-esp32-s3-supermini-${{ github.ref_name }}.bin
            partition-table-esp32-s3-supermini-${{ github.ref_name }}.bin
            spiffs-esp32-s3-supermini-${{ github.ref_name }}.bin
